# AsyncHttp API

Asynchronously make calls to http services.

## cancel

Cancel the current AsyncHttp call.

## download_file

Download a file to the specified filename.

Syntax: download_file(url, headers, filename, callback, callback_param)

url – URL of the request.  
headers – Hash of headers to send with the request.  
callback – Callback action to execute when the request is done.   
callback_param – (optional) Parameters to send to the callback. Parameters values should be url encoded with RhoSupport.url_encode.  
ZZZZZZZZ put in link to RhoSupport  
authentication – (optional) Send Basic Auth header with request.
ssl_verify_peer – (optional) Verify SSL certificates. Default = true.

The authentication parameter takes the form:

	:::ruby
	:authentication => {
	  :type => :basic, 
	  :username => "john", 
	  :password => "secret"
	}

## get

Perform an HTTP GET request to the specified URL.

Syntax: get(url, headers, callback)

url – URL of the request.  
headers – Hash of headers to send with the request.  
callback – Callback action to execute when the request is done.   
callback_param – (optional) Parameters to send to the callback. Parameters values should be url encoded with RhoSupport.url_encode.  
ZZZZZZZZ put in link to RhoSupport  
authentication – (optional) Send Basic Auth header with request.
ssl_verify_peer – (optional) Verify SSL certificates. Default = true.

The authentication parameter takes the form:

	:::ruby
	:authentication => {
	  :type => :basic, 
	  :username => "john", 
	  :password => "secret"
	} 

## post

Perform HTTP POST request to the specified URL.

Syntax: post(url, headers, callback)

url – URL of the request.  
headers – Hash of headers to send with the request.  
callback – Callback action to execute when the request is done.   
callback_param – (optional) Parameters to send to the callback. Parameters values should be url encoded with RhoSupport.url_encode.  
ZZZZZZZZ put in link to RhoSupport  
authentication – (optional) Send Basic Auth header with request.
:ssl_verify_peer – (optional) Verify SSL certificates, true by default.

The authentication parameter takes the form:

	:::ruby
	:authentication => {
	  :type => :basic, 
	  :username => "john", 
	  :password => "secret"
	}

## upload_file

Upload the specified file using HTTP POST.

Syntax: upload_file(:url, :headers, :filename, :body, :callback, :callback_param)

url – URL of the request.  
headers – Hash of headers to send with the request.  
callback – Callback action to execute when the request is done.   
callback_param – (optional) Parameters to send to the callback. Parameters values should be url encoded with RhoSupport.url_encode.  
ZZZZZZZZ put in link to RhoSupport.url_encode  
authentication – (optional) Send Basic Auth header with request.
:ssl_verify_peer – (optional) Verify SSL certificates, true by default.

The authentication parameter takes the form:

	:::ruby
	:authentication => {
	  :type => :basic, 
	  :username => "john", 
	  :password => "secret"
	}

